import React from "react";
import ContactForm from "./ContactForm";

const page = () => {
  return (
    <div className="flex-1 max-w-xs w-full mx-auto flex items-center">
      <ContactForm />
    </div>
  );
};

export default page;
